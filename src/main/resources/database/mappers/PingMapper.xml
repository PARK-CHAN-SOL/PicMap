<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- mapper는 테이블 별로 하나씩 만들 거다. namespace는 중복되면 안된다. -->
<mapper namespace="com.picmap.app.ping.PingDAO">
	<insert id="addPing" parameterType="PingDTO">
		INSERT INTO PING
		VALUES (PING_SEQ.NEXTVAL, #{longitude}, #{latitude}, #{address})
	</insert>
	
	<select id="searchPing" parameterType="PingDTO" resultType="PingDTO">
		SELECT	LATITUDE, LONGITUDE
		FROM	PING
		<where>
			ADDRESS LIKE '%'||#{address}||'%'
		</where>
	</select>
</mapper>